import searchParams from './reducer';

import * as searchParamsTypes from './types';
import * as searchParamsActions from './actions';
import * as searchParamsSelectors from './selectors';

export { searchParamsTypes, searchParamsActions, searchParamsSelectors };
export default searchParams;
