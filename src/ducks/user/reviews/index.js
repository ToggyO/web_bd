import reviews from './reducer';
import * as reviewsTypes from './types';
import * as reviewsActions from './actions';
import * as reviewsSagas from './sagas';
import * as reviewsSelectors from './selectors';

export { reviewsTypes, reviewsActions, reviewsSagas, reviewsSelectors };

export default reviews;
