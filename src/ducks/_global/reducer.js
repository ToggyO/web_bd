/* eslint-disable no-underscore-dangle */
const initialState = {
  loading: false,
};

export default function _global(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
